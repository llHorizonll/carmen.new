# API Spec

Canonical doc: `docs/api/api-spec.md`

This project treats PocketBase collections as the API surface.

Use tenant-scoped queries, audit all mutations, and subscribe to realtime collection changes for live UI updates.

