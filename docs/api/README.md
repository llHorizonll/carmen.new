# API

This folder describes the PocketBase-facing API contract the frontend should follow.

Read `api-spec.md` first.

The API layer should define:
- collection names
- query shapes
- realtime subscription targets
- tenant-scoped reads and writes

